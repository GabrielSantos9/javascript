function as() {
   var numin =  window.prompt('Digite um número inteiro qualquer')
   var menos = 1
   var mais = 1
   var soma = Number(numin)+Number(menos)
   var multiplicacao = Number(numin)-Number(mais)
   window.alert(`Antes de ${numin}, temos o número ${multiplicacao}
Depois de ${numin} temos o número ${soma}`)
}